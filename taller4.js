let Servicio = parseFloat(prompt("increse el tipo de servicio: 1 .lavado 2.planchado"));
let tipoLavado = parseFloat(prompt("ingrese el tipo de lavado ( 1. Seco o 2. Normal):"));
let prendas = parseFloat(prompt("increse la cantidad de prendas"));
let costo = 0;

 
if (Servicio === "1") {
    prendas = costo  * 2000;
    alert("precio es")
} else if (Servicio === "2") {
    prendas = costo && planchado * 1800;
    alert("precio es")
        
} else {
    alert("Adicional se cobra un domicilio de 3000 solo si es planchado; para otros servicios es gratis");
        
}
    

